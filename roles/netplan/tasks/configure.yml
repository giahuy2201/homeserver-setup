---
- name: Config netplan
  copy:
    src: netplan.yml
    dest: /etc/netplan/00-ansible.yaml
    owner: root
    group: root
    mode: 0644
  notify: apply netplan

- name: Apply netplan config
  command: netplan apply